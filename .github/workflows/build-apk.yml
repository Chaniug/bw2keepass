name: Build Android APK

on:
  push:
    branches: [main]
    paths:
      - 'app/**'
      - '.github/workflows/build-apk.yml'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build-apk:
    runs-on: ubuntu-latest
    env:
      ANDROID_HOME: /usr/local/lib/android/sdk
    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          fetch-depth: 1

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Setup Android SDK
        # 不缓存 Android SDK：SDK 目录含带 Linux capabilities/xattr 的文件
        # （如 platform-tools/adb），actions/cache 用 tar 恢复时无法还原这些
        # 扩展属性，导致 "tar failed with exit code 2"。改为每次全新安装，
        # 安装步骤本身幂等（先 rm -rf 残留目录再 sdkmanager）。
        run: |
          mkdir -p $ANDROID_HOME/cmdline-tools
          cd $ANDROID_HOME/cmdline-tools
          curl -fsSL https://dl.google.com/android/repository/commandlinetools-linux-11076708_latest.zip -o tools.zip
          unzip -q -o tools.zip
          # 幂等：清掉上一次安装遗留的 latest，避免 mv 到非空目录冲突
          rm -rf latest
          mv cmdline-tools latest
          yes | $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager --licenses > /dev/null 2>&1 || true
          # 幂等：清掉上一次安装遗留的目录，确保 sdkmanager 干净重装
          rm -rf $ANDROID_HOME/platforms/android-34 $ANDROID_HOME/build-tools/34.0.0 $ANDROID_HOME/platform-tools
          $ANDROID_HOME/cmdline-tools/latest/bin/sdkmanager "platforms;android-34" "build-tools;34.0.0" "platform-tools"

      - name: Verify Android project (固化的 app/ 工程)
        run: |
          echo "使用仓库内固化的 app/ 工程，无需 CI 临时拼装"
          test -f app/src/main/AndroidManifest.xml && echo "  OK AndroidManifest.xml"
          test -f app/src/main/java/cc/valk/pass2kdbx/MainActivity.java && echo "  OK MainActivity.java"
          test -f app/src/main/res/values/styles.xml && echo "  OK styles.xml"
          test -f app/src/main/res/drawable/ic_launcher.xml && echo "  OK ic_launcher.xml"
          test -f app/src/main/assets/index.html && echo "  OK assets/index.html"

      - name: Prepare signing keystore
        # 优先使用仓库 Secret RELEASE_KEYSTORE_B64（固定 keystore，签名稳定、可覆盖安装）；
        # 未配置时临时生成，仅用于本地/测试构建，签名不固定。
        # 配置位置：仓库 Settings → Secrets and variables → Actions → New repository secret
        #           （Name=RELEASE_KEYSTORE_B64，Value=固定 keystore 的 base64）。
        # 若从聊天/网页复制 base64 时混入换行(\r\n)、空格等空白字符，GNU base64 -d 会报
        # "invalid input"；这里先 tr 去除空白再解码，并给出明确报错便于排查。
        env:
          KEYSTORE_B64: ${{ secrets.RELEASE_KEYSTORE_B64 }}
        run: |
          mkdir -p app/build
          if [ -n "$KEYSTORE_B64" ]; then
            # 去除复制粘贴可能带入的空白字符（\r \n 空格 \t），避免 base64 解码失败
            SANITIZED=$(printf '%s' "$KEYSTORE_B64" | tr -d '\r\n \t')
            if printf '%s' "$SANITIZED" | base64 -d > app/build/release.keystore 2>/dev/null; then
              # 解码成功后再校验 keystore 可用性（固定密码 pass2kdbx_release），
              # 提前暴露“Secret 被截断”或“密码不匹配”等问题，避免到 apksigner 才报错。
              if keytool -list -keystore app/build/release.keystore -storepass pass2kdbx_release >/dev/null 2>&1; then
                echo "  使用仓库 Secret 中的固定 keystore（签名稳定，可覆盖安装）"
              else
                echo "  ⚠️ keystore 解码成功但校验失败：Secret 可能被截断，或 keystore 密码非 pass2kdbx_release"
                exit 1
              fi
            else
              echo "  ⚠️ RELEASE_KEYSTORE_B64 解码失败：请确认 Secret 为完整、无换行/空格的 base64"
              echo "     （可由本地 keytool 生成 keystore 后执行： base64 -w0 release.keystore ）"
              exit 1
            fi
          else
            echo "  未配置 RELEASE_KEYSTORE_B64，临时生成 keystore（仅测试，签名不固定）"
            keytool -genkey -v -keystore app/build/release.keystore \
              -alias release -keyalg RSA -keysize 2048 -validity 10000 \
              -storepass pass2kdbx_release -keypass pass2kdbx_release \
              -dname "CN=Pass2KDBX, OU=App, O=Valk, L=Unknown, S=Unknown, C=US" \
              -noprompt
          fi

      - name: Sync web/ source into app assets
        run: |
          cp web/index.html app/src/main/assets/index.html
          cp web/engine.js app/src/main/assets/engine.js
          mkdir -p app/src/main/assets/vendor
          cp web/vendor/* app/src/main/assets/vendor/
          cp web/icon.svg app/src/main/assets/icon.svg 2>/dev/null || true

      - name: Build APK
        env:
          VERSION_CODE: 3
          VERSION_NAME: 3.0
        run: |
          BUILD_TOOLS=$ANDROID_HOME/build-tools/34.0.0
          PLATFORM=$ANDROID_HOME/platforms/android-34
          mkdir -p app/build

          # 1. 编译资源
          $BUILD_TOOLS/aapt2 compile -o app/build/res.zip --dir app/src/main/res

          # 2. 链接资源生成 base.apk（含 assets）
          $BUILD_TOOLS/aapt2 link -o app/build/base.apk \
            -I $PLATFORM/android.jar \
            --manifest app/src/main/AndroidManifest.xml \
            --java app/build/gen \
            -A app/src/main/assets \
            --min-sdk-version 21 \
            --target-sdk-version 34 \
            --version-code ${VERSION_CODE} \
            --version-name ${VERSION_NAME} \
            --package-id 0x7f \
            app/build/res.zip

          echo "=== base.apk 内容 ==="
          unzip -l app/build/base.apk

          # 3. 编译 Java
          mkdir -p app/build/obj
          javac -source 17 -target 17 \
            -cp $PLATFORM/android.jar \
            -d app/build/obj \
            app/src/main/java/cc/valk/pass2kdbx/MainActivity.java \
            app/build/gen/cc/valk/pass2kdbx/R.java

          # 4. DEX 编译
          mkdir -p app/build/dex
          $BUILD_TOOLS/d8 --lib $PLATFORM/android.jar \
            --output app/build/dex \
            app/build/obj/cc/valk/pass2kdbx/*.class

          # 5. 将 classes.dex 直接添加到 base.apk（不重新打包，保持 aapt2 的 ZIP 结构）
          cp app/build/base.apk app/build/unsigned.apk
          cd app/build/dex
          zip -0 -j ../unsigned.apk classes.dex
          cd ../../..

          echo "=== unsigned.apk 内容 ==="
          unzip -l app/build/unsigned.apk

          # 6. 对齐（必须在签名之前）
          $BUILD_TOOLS/zipalign -p -f 4 app/build/unsigned.apk app/build/aligned.apk

          # 7. 签名（使用仓库 Secret 注入的固定 keystore，保证签名一致可覆盖安装）
          $BUILD_TOOLS/apksigner sign \
            --ks app/build/release.keystore \
            --ks-pass pass:pass2kdbx_release \
            --key-pass pass:pass2kdbx_release \
            --v1-signing-enabled true \
            --v2-signing-enabled true \
            --v3-signing-enabled true \
            --out app/build/Pass2KDBX.apk \
            app/build/aligned.apk

          # 9. 验证签名
          echo "=== 验证 APK 签名 ==="
          $BUILD_TOOLS/apksigner verify --verbose app/build/Pass2KDBX.apk || true

          echo "=== 最终 APK 信息 ==="
          ls -lh app/build/Pass2KDBX.apk
          echo "=== APK 内容列表 ==="
          unzip -l app/build/Pass2KDBX.apk

      - name: Upload APK as Artifact
        # v6 起默认运行于 Node.js 24，消除 Node20 弃用告警
        uses: actions/upload-artifact@v6
        with:
          name: Pass2KDBX-APK
          path: app/build/Pass2KDBX.apk

      - name: Create/Update Release (独立 app-v* tag)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # 独立发布线：tag 前缀 app-v*，不与网页版 apk-latest 混用
          APP_TAG="app-v${VERSION_NAME:-1.0}"
          # 若 tag 已存在则先清理（便于重跑）
          gh release delete "$APP_TAG" --yes --cleanup-tag 2>/dev/null || true
          git push origin ":refs/tags/$APP_TAG" 2>/dev/null || true
          git tag -d "$APP_TAG" 2>/dev/null || true
          # 让 gh 直接在远端基于当前 commit 创建 tag，避免"本地 tag 未推送"竞态
          gh release create "$APP_TAG" \
            app/build/Pass2KDBX.apk \
            --target "${{ github.sha }}" \
            --title "Pass2KDBX Android App v${VERSION_NAME:-1.0}" \
            --notes "## Pass2KDBX Android App

          自动构建的 Android APK，可直接安装。

          - 版本：${VERSION_NAME:-1.0} (code ${VERSION_CODE:-1})
          - 构建时间：$(date -u '+%Y-%m-%d %H:%M UTC')
          - 提交：${{ github.sha }}
          - 安装：下载 APK → 允许\"未知来源\"安装 → 打开使用

          > 此 Release 为 Android App 独立发布线（tag: $APP_TAG）。" \
            --latest
          # 同步本地 tag 与远端保持一致
          git fetch origin "refs/tags/$APP_TAG:refs/tags/$APP_TAG" 2>/dev/null || true
